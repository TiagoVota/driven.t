import styled from 'styled-components';

const HotelContainer = styled.div`
  display: flex;
  align-items: center;
  flex-direction: row;
  margin-top: 18px;
  width: 100%;
  gap: 19px;
  cursor: pointer;
`;

export default HotelContainer;
